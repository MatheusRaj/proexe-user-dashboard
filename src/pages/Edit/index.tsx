import React, { memo } from 'react';
import Container from 'components/Container';

const Edit = memo(() => {
    return (
        <Container>
            <div>Edit</div>
        </Container>
    )
})

export default Edit;
