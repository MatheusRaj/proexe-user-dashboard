import React, { memo } from 'react';
import Container from 'components/Container';

const New = memo(() => {
    return (
        <Container>
            <div>New</div>
        </Container>
    )
})

export default New;
